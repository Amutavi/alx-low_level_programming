#include <stdio.h>

/**
 * main - print all possible different combinations of three digits
 *Return: Always 0 (success)
 */
int main(void)
{
  int n, m, l;

  for (n = 48; n < 58; m++)
    {
      for (1 = 50; 1 < 58; 1++)
	{
	  putchar(n);
	  putchar(m);
	  putchar(1)
	    if (n != 55 || m != 56)
	      {
		putchar(',');
		putchar((' ');
			}
	      }
	}
    }
}
putchar